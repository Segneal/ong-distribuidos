# Email service configuration
EMAIL_SERVICE_PORT=3001

# SMTP configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=noreply@empujecomunitario.org
SMTP_PASS=your_app_password

# For testing, leave SMTP_USER and SMTP_PASS empty to simulate email sending